import {default as MUICloseIcon} from '@mui/icons-material/Close';
import {IconsType} from '../';

export const CloseIcon: React.FC<IconsType> = (props) => <MUICloseIcon fontSize={props?.fontSize} color='inherit' />
  
