import {default as MUISearchIcon} from '@mui/icons-material/Search';
import {IconsType} from '../';

export const SearchIcon: React.FC<IconsType> = (props) => <MUISearchIcon fontSize={props?.fontSize} color='inherit' />