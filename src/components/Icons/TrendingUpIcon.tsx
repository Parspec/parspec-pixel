import { TrendingUp } from '@mui/icons-material/';
import { IconProps } from '.';

export const TrendingUpIcon: React.FC<IconProps>  = ({fontSize}) => {
  return (
    <TrendingUp fontSize={fontSize} />
  )
}