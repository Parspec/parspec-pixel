import { UnfoldMore } from '@mui/icons-material/';
import { IconProps } from '.';

export const UnfoldMoreIcon: React.FC<IconProps> = ({fontSize}) => {
  return (
    <UnfoldMore fontSize={fontSize} />
  )
}